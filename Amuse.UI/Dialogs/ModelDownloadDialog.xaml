<local:BaseDialog x:Class="Amuse.UI.Dialogs.ModelDownloadDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Amuse.UI.Dialogs"
        xmlns:controls="clr-namespace:Amuse.UI.UserControls"
        mc:Ignorable="d"
        x:Name="UI"
        Icon="/Images/Icon.png"
        MinWidth="500"
        MaxWidth="500"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner" 
        SnapsToDevicePixels="True"
        AllowsTransparency="True"
        WindowStyle="None"
        Title="Model Download"
        Style="{StaticResource BaseToolWindow}">
    <DockPanel DataContext="{Binding ElementName=UI}" Margin="15, 15, 15, 10">
        <DockPanel DockPanel.Dock="Bottom"  Margin="0,20,0,0" LastChildFill="False">
            <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">

                <Button Style="{StaticResource TransparentButton}" Command="{Binding ChangeLocationCommand}">
                    <StackPanel Orientation="Horizontal" Margin="4, 0">
                        <controls:FontAwesome Icon="&#xe053;" IconStyle="Thin"/>
                        <TextBlock Text="Change Download Location" Margin="4,1,0,0" Opacity=".9"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <UniformGrid DockPanel.Dock="Right" Columns="2" Height="30">
                <Button Content="Download" Command="{Binding DownloadCommand}" IsDefault="True"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100"/>
            </UniformGrid>
        </DockPanel>

        <DockPanel>
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,10">
                <TextBlock Text="Amuse needs to download some model files from the internet" FontSize="14"  TextWrapping="Wrap" TextAlignment="Center"/>
            </StackPanel>

            <Border DockPanel.Dock="Top" Visibility="{Binding ErrorMessage, Converter={StaticResource NullOrEmptyVisibilityConverter}}" Padding="10,10" Background="{StaticResource DangerColour}" Opacity=".7">
                <TextBlock Text="{Binding ErrorMessage}" Foreground="White" TextWrapping="Wrap" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>


            <StackPanel DockPanel.Dock="Bottom" Margin="0,10,0,5">
                <TextBlock DockPanel.Dock="Top" Text="I Accept the terms &amp; conditions of the following models:" Opacity=".7" HorizontalAlignment="Center"/>
                <DockPanel  HorizontalAlignment="Center">
                    <CheckBox DockPanel.Dock="Right" IsChecked="{Binding IsLicenceAccepted}" Style="{StaticResource SwitchBox}" Tag="Required" VerticalAlignment="Center" Margin="12,3,0,0"/>
                    <ItemsControl ItemsSource="{Binding ModelTemplates}" HorizontalAlignment="Center" IsEnabled="{Binding IsDownloading, Converter={StaticResource InverseBoolConverter}}" VerticalAlignment="Center">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0" HorizontalAlignment="Right">
                                    <TextBlock Text="{Binding Name}" Margin="0,0,5,0" HorizontalAlignment="Right"/>
                                    <controls:HyperlinkControl DockPanel.Dock="Bottom" Link="{Binding Licence}" Label="(Licence)" HorizontalAlignment="Right" ToolTipService.InitialShowDelay="300">
                                        <controls:HyperlinkControl.ToolTip>
                                            <StackPanel>
                                                <TextBlock Text="External Link:" Style="{StaticResource FieldTextBlockStyle}" />
                                                <TextBlock Text="{Binding Licence}" Margin="0,0,0,4"/>
                                            </StackPanel>
                                        </controls:HyperlinkControl.ToolTip>
                                    </controls:HyperlinkControl>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </DockPanel>
            </StackPanel>


            <ItemsControl ItemsSource="{Binding ModelTemplates}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <DockPanel Margin="0,10,0,10">
                            <Border DockPanel.Dock="Top" Background="{StaticResource ContainerBackground}" BorderBrush="{StaticResource ControlDarkerBorderBrush}" BorderThickness="1" Padding="4">
                                <DockPanel >
                                    <controls:CachedMedia 
                                        DockPanel.Dock="Left" 
                                        Width="90" 
                                        Height="90" 
                                        VerticalAlignment="Top"
                                        CacheName="{Binding Id}" 
                                        MediaUrl="{Binding ImageIcon}"/>
                                    <StackPanel Margin="5,0,0,0">
                                        <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding Description}" TextWrapping="WrapWithOverflow"  FontSize="11"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>

                            <DockPanel DockPanel.Dock="Bottom" LastChildFill="False">
                                <TextBlock DockPanel.Dock="Left" Style="{StaticResource FieldTextBlockStyle}">
                                    <Run Text="{Binding DownloadInfo.DownloadAmount, Mode=OneWay, FallbackValue=0 GB, Converter={StaticResource DownloadAmountConverter}}" />
                                    <Run Text="\" />
                                    <Run Text="{Binding DownloadSize, Mode=OneWay, FallbackValue=0 GB, StringFormat={}{0:F2} GB}"  />
                                    <Run Text="  @  " />
                                    <Run Text="{Binding DownloadInfo.DownloadSpeed, Mode=OneWay, FallbackValue=0 b/s, Converter={StaticResource DownloadSpeedConverter}}"  />
                                </TextBlock>
                                <TextBlock DockPanel.Dock="Right" Style="{StaticResource FieldTextBlockStyle}">
                                   <Run Text="Time Remaining: " />
                                   <Run Text="{Binding DownloadInfo.TimeRemaining, Mode=OneWay, FallbackValue=0 sec, Converter={StaticResource DownloadTimeConverter}}"  />
                                </TextBlock>
                            </DockPanel>
                            <ProgressBar Value="{Binding ProgressValue}" Minimum="1" Maximum="100" Height="20"/>
                        </DockPanel>

                    </DataTemplate>
                </ItemsControl.ItemTemplate>

            </ItemsControl>
        </DockPanel>
    </DockPanel>
</local:BaseDialog>