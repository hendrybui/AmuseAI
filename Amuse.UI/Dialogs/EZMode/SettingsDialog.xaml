<local:BaseDialog x:Class="Amuse.UI.Dialogs.EZMode.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Amuse.UI.Dialogs"
        xmlns:controls="clr-namespace:Amuse.UI.UserControls"
        mc:Ignorable="d"
        x:Name="UI"
        Icon="/Images/Icon.png"
        MinWidth="500"
        MaxWidth="500"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner" 
        SnapsToDevicePixels="True"
        AllowsTransparency="True"
        WindowStyle="None"
        Title="Settings"
        Style="{StaticResource BaseToolWindow}">
    <DockPanel DataContext="{Binding ElementName=UI}" Margin="15, 15, 15, 10">
        <DockPanel DockPanel.Dock="Bottom"  Margin="0,20,0,0">

            <UniformGrid DockPanel.Dock="Right" Columns="2" Height="30">
                <Button Content="Ok" Command="{Binding SaveCommand}" IsDefault="True"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100"/>
            </UniformGrid>

            <UniformGrid Columns="2" HorizontalAlignment="Left">
                <Button Command="{Binding OpenDirectoryCommand}" CommandParameter="{Binding LogDirectory, Source={x:Static Application.Current}}"  Width="40" Height="30" HorizontalAlignment="Left" ToolTip="Open Log Directory">
                    <controls:FontAwesome Icon="&#xf56f;" Margin="0,0,4,0"/>
                </Button>
                <Button Command="{Binding OpenDirectoryCommand}" CommandParameter="{Binding Settings.DirectoryModel}"  Width="40" Height="30" HorizontalAlignment="Left" ToolTip="Open Models Directory">
                    <controls:FontAwesome Icon="&#xf5fd;" Margin="0,0,4,0"/>
                </Button>
            </UniformGrid>

        </DockPanel>

        <DockPanel>
            <StackPanel>

                <!--Model Directory-->
                <DockPanel>
                    <Grid DockPanel.Dock="Left" >
                        <StackPanel>
                            <TextBlock Text="Model Download Directory" Style="{StaticResource FieldTextBlockStyle}"/>
                            <controls:FilePickerTextBox IsFolderPicker="True" FileName="{Binding Settings.DirectoryModel, Mode=TwoWay}" IsRequired="True"/>
                        </StackPanel>
                    </Grid>
                </DockPanel>
                <TextBlock Text="Prompt Options" Style="{StaticResource FieldTextBlockStyle}" Margin="0,15,0,0"/>

                <Grid HorizontalAlignment="Left">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Enable Negative Prompt" Opacity=".9" Margin="0,2,5,0" />
                    <CheckBox Grid.Row="0" Grid.Column="1" IsChecked="{Binding IsNegativePromptEnabled}" ToolTip="Enable the Negative Prompt input" HorizontalAlignment="Left"  Style="{StaticResource SwitchBox}" Margin="0,0,0,3"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Enable Prompt Enhancement" Opacity=".9" Margin="0,2,5,0" />
                    <CheckBox Grid.Row="1" Grid.Column="1" IsChecked="{Binding IsPromptEnhanceEnabled}" ToolTip="Adds common prompt tags to try improve image output quality" HorizontalAlignment="Left"  Style="{StaticResource SwitchBox}"/>
                </Grid>

            </StackPanel>

        </DockPanel>

    </DockPanel>
</local:BaseDialog>