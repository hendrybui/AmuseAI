<local:BaseDialog x:Class="Amuse.UI.Dialogs.AddUpscaleModelDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Amuse.UI.Dialogs"
        xmlns:models="clr-namespace:Amuse.UI.Models"
        xmlns:userControls="clr-namespace:Amuse.UI.UserControls"
        mc:Ignorable="d"
        x:Name="UI"
        Icon="/Images/Icon.png"
        MinWidth="500"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner" 
        SnapsToDevicePixels="True"
        AllowsTransparency="True"
        WindowStyle="None"
        Style="{StaticResource BaseToolWindow}"
        Title="Add Upscale Model">
    <DockPanel DataContext="{Binding ElementName=UI}" Margin="15, 5, 15, 10">
        <StackPanel DockPanel.Dock="Top">

            <StackPanel Visibility="{Binding EnableTemplateSelection, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="Model Type" Style="{StaticResource FieldTextBlockStyle}"/>
                <ComboBox ItemsSource="{Binding ModelTemplates}" SelectedItem="{Binding ModelTemplate}" DisplayMemberPath="Name" >
                    <ComboBox.ItemContainerStyle>
                        <Style TargetType="{x:Type ComboBoxItem}" BasedOn="{StaticResource {x:Type ComboBoxItem}}">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Category}" Value="Upscaler">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsUserTemplate}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>

                        </Style>
                    </ComboBox.ItemContainerStyle>
                </ComboBox>
            </StackPanel>



            <StackPanel Margin="0,0,0,0">
                <TextBlock Text="Model File" Style="{StaticResource FieldTextBlockStyle}"/>
                <userControls:FilePickerTextBox FileName="{Binding ModelFile, Mode=TwoWay}" InitialDirectory="{Binding Settings.DirectoryModel}" IsFolderPicker="False" Filter="Onnx Models (*.onnx)|*.onnx" DefaultExt="onnx" />
            </StackPanel>

            <StackPanel Visibility="{Binding EnableNameSelection, Converter={StaticResource BooleanToVisibilityConverter}}">

                <DockPanel Margin="0,5,0,0" >
                    <StackPanel >
                        <TextBlock Text="Model Name" Style="{StaticResource FieldTextBlockStyle}"/>
                        <TextBox Text="{Binding ModelName, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </DockPanel>


                <UniformGrid Columns="4"  Margin="0,5,0,0">
                    <StackPanel Margin="0,0,0,0">
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Right" Text="(0=Any)" Style="{StaticResource FieldTextBlockStyle}" FontSize="8" VerticalAlignment="Bottom" Margin="0,0,2,2"/>
                            <TextBlock Text="Sample Size "  Style="{StaticResource FieldTextBlockStyle}"/>
                        </DockPanel>
                        <TextBox Text="{Binding UpscaleTemplate.SampleSize}" />
                    </StackPanel>

                    <StackPanel Margin="5,0,0,0">
                        <TextBlock Text="Scale Factor" Style="{StaticResource FieldTextBlockStyle}" />
                        <TextBox Text="{Binding UpscaleTemplate.ScaleFactor}" />
                    </StackPanel>

                    <StackPanel Margin="5,0,0,0">
                        <TextBlock Text="Channels" Style="{StaticResource FieldTextBlockStyle}" />
                        <TextBox Text="{Binding UpscaleTemplate.Channels}" />
                    </StackPanel>

                    <StackPanel Margin="5,0,0,0">
                        <TextBlock Text="Normalize Type" Style="{StaticResource FieldTextBlockStyle}" />
                        <ComboBox ItemsSource="{Binding Source={StaticResource ImageNormalizeType}}" SelectedItem="{Binding UpscaleTemplate.NormalizeType}" />
                    </StackPanel>
                </UniformGrid>


                <UniformGrid DockPanel.Dock="Top" Columns="3" Margin="0,2,0,0" >
                    <StackPanel Margin="1,0,1,0" ToolTip="Upscale image in smaller tiles to save memory">
                        <TextBlock Style="{StaticResource FieldTextBlockStyle}">Default Tile Mode</TextBlock>
                        <ComboBox ItemsSource="{Binding Source={StaticResource TileMode}}" SelectedItem="{Binding UpscaleTemplate.TileMode}" Style="{StaticResource EnumComboBox}"/>
                    </StackPanel>
                    <StackPanel ToolTip="The maximum tile size to use">
                        <TextBlock Text="Default Tile Size" Style="{StaticResource FieldTextBlockStyle}"/>
                        <TextBox Text="{Binding UpscaleTemplate.TileSize, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding TileMode, Converter={StaticResource InverseEnumToBoolConverter}, ConverterParameter=None}" />
                    </StackPanel>
                    <StackPanel ToolTip="Pixels to overlap when joining tiles, reduces visible seams">
                        <TextBlock Text="Default Tile Overlap" Style="{StaticResource FieldTextBlockStyle}"/>
                        <TextBox Text="{Binding UpscaleTemplate.TileOverlap, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding TileMode, Converter={StaticResource InverseEnumToBoolConverter}, ConverterParameter=None}" Margin="4,0,0,0"/>
                    </StackPanel>
                </UniformGrid>



            </StackPanel>
            <StackPanel Margin="5">


                <ItemsControl ItemsSource="{Binding ValidationResults}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:ValidationResult}">
                            <StackPanel Orientation="Horizontal">
                                <userControls:FontAwesome >
                                    <userControls:FontAwesome.Style>
                                        <Style TargetType="{x:Type userControls:FontAwesome}">
                                            <Setter Property="Color" Value="Red" />
                                            <Setter Property="Icon" Value="&#xf00d;" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsValid}" Value="True">
                                                    <Setter Property="Color" Value="LimeGreen" />
                                                    <Setter Property="Icon" Value="&#xf00c;" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </userControls:FontAwesome.Style>
                                </userControls:FontAwesome>
                                <TextBlock Text="{Binding Name}" Margin="5,0,0,3" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </StackPanel>


        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <UniformGrid Columns="2" Height="30">
                <Button Content="Save" Command="{Binding SaveCommand}" IsDefault="True"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100"/>
            </UniformGrid>
        </StackPanel>
    </DockPanel>
</local:BaseDialog>