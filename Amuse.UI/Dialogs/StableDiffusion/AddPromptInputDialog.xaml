<local:BaseDialog x:Class="Amuse.UI.Dialogs.AddPromptInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Amuse.UI.Dialogs"
        mc:Ignorable="d"
        x:Name="UI"
        Icon="/Images/Icon.png"
        MinWidth="500"
        MaxWidth="500"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner" 
        SnapsToDevicePixels="True"
        AllowsTransparency="True"
        WindowStyle="None"
        Style="{StaticResource BaseToolWindow}">
    <DockPanel DataContext="{Binding ElementName=UI}" Margin="15, 5, 15, 10">

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <UniformGrid Columns="2" Height="30">
                <Button Content="Ok" Command="{Binding SaveCommand}" IsDefault="True"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100"/>
            </UniformGrid>
        </StackPanel>

        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,5">
            <StackPanel DockPanel.Dock="Left" MinWidth="120">
                <TextBlock Text="Prompt Type" Style="{StaticResource FieldTextBlockStyle}" />
                <ComboBox SelectedItem="{Binding PromptInputType}" ItemsSource="{Binding Source={StaticResource PromptInputType}}" />
            </StackPanel>

            <TextBlock />
        </DockPanel>

        <TextBlock DockPanel.Dock="Top" Text="Prompt Text" Style="{StaticResource FieldTextBlockStyle}" />
        <TextBox Text="{Binding PromptText, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" MinHeight="150"></TextBox>
    </DockPanel>
</local:BaseDialog>