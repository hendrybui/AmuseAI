<local:BaseDialog x:Class="Amuse.UI.Dialogs.ModelLicenceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Amuse.UI.Dialogs"
        xmlns:controls="clr-namespace:Amuse.UI.UserControls"
        mc:Ignorable="d"
        x:Name="UI"
        Icon="/Images/Icon.png"
        MinWidth="500"
        MaxWidth="500"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner" 
        SnapsToDevicePixels="True"
        AllowsTransparency="True"
        WindowStyle="None"
        Title="Action Required"
        Style="{StaticResource BaseToolWindow}">
    <DockPanel DataContext="{Binding ElementName=UI}" Margin="15, 15, 15, 10">

        <DockPanel DockPanel.Dock="Bottom"  Margin="0,20,0,0" LastChildFill="False">
            <UniformGrid DockPanel.Dock="Right" Columns="2" Height="30">
                <Button Content="Continue" Command="{Binding SaveCommand}" IsDefault="True"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100"/>
            </UniformGrid>
        </DockPanel>

        <DockPanel>
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,10">
                <TextBlock Text="{Binding ModelTemplate.Name, StringFormat={}Please review and accept the terms and conditions to proceed with downloading {0}}" FontSize="14"  TextWrapping="Wrap" TextAlignment="Center"/>
                <controls:HyperlinkControl DockPanel.Dock="Bottom"  Link="{Binding ModelTemplate.Licence}" Label="{Binding ModelTemplate.Name, StringFormat={}View {0} Licence}" FontSize="14" HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>

            <Grid HorizontalAlignment="Center">
                <StackPanel DockPanel.Dock="Bottom"  >
                    <DockPanel Margin="0,8,0,0" LastChildFill="False">
                        <CheckBox DockPanel.Dock="Right" IsChecked="{Binding ModelTemplate.IsLicenceAccepted}" Style="{StaticResource SwitchBox}" Tag="Required"/>
                        <TextBlock Margin="0,2,5,0" >
                             <Run Text="I Accept the terms &amp; conditions of" />
                             <Run Text="{Binding ModelTemplate.Name, Mode=OneWay}" /> 
                        </TextBlock>
                    </DockPanel>
                  
                </StackPanel>
            </Grid>

        </DockPanel>
    </DockPanel>
</local:BaseDialog>