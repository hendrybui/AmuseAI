<local:BaseDialog x:Class="Amuse.UI.Dialogs.AddFeatureExtractorModelDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Amuse.UI.Dialogs"
        xmlns:models="clr-namespace:Amuse.UI.Models"
        xmlns:userControls="clr-namespace:Amuse.UI.UserControls"
        mc:Ignorable="d"
        x:Name="UI"
        Icon="/Images/Icon.png"
        MinWidth="500"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner" 
        SnapsToDevicePixels="True"
        AllowsTransparency="True"
        WindowStyle="None"
        Style="{StaticResource BaseToolWindow}" 
        Title="Add Feature Extractor Model">
    <DockPanel DataContext="{Binding ElementName=UI}" Margin="15, 5, 15, 10">
        <StackPanel DockPanel.Dock="Top">

            <StackPanel Visibility="{Binding EnableTemplateSelection, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="Model Type" Style="{StaticResource FieldTextBlockStyle}"/>
                <ComboBox ItemsSource="{Binding ModelTemplates}" SelectedItem="{Binding ModelTemplate}" DisplayMemberPath="Name" >
                    <ComboBox.ItemContainerStyle>
                        <Style TargetType="{x:Type ComboBoxItem}" BasedOn="{StaticResource {x:Type ComboBoxItem}}">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Category}" Value="FeatureExtractor">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsUserTemplate}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ComboBox.ItemContainerStyle>
                </ComboBox>
            </StackPanel>


            <StackPanel Margin="0,0,0,0">
                <TextBlock Text="Model File" Style="{StaticResource FieldTextBlockStyle}"/>
                <userControls:FilePickerTextBox FileName="{Binding ModelFile, Mode=TwoWay}" IsFolderPicker="False" Filter="Onnx Models (*.onnx)|*.onnx" DefaultExt="onnx" />
            </StackPanel>


            <StackPanel Visibility="{Binding EnableNameSelection, Converter={StaticResource BooleanToVisibilityConverter}}">

                <DockPanel Margin="0,5,0,0">
                    <StackPanel >
                        <TextBlock Text="Model Name" Style="{StaticResource FieldTextBlockStyle}"/>
                        <TextBox Text="{Binding ModelName, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </DockPanel>


                <UniformGrid Columns="3" Margin="0,5,0,0">
                    <StackPanel Margin="0,0,0,0">
                        <TextBlock Text="Sample Size" Style="{StaticResource FieldTextBlockStyle}"/>
                        <TextBox Text="{Binding FeatureExtractorTemplate.SampleSize}" />
                    </StackPanel>

                    <StackPanel Margin="5,0,0,0">
                        <TextBlock Text="OutputChannels" Style="{StaticResource FieldTextBlockStyle}"/>
                        <TextBox Text="{Binding FeatureExtractorTemplate.OutputChannels}" />
                    </StackPanel>

                    <StackPanel Margin="5,0,0,0">
                        <CheckBox Content="ControlNet Support" IsChecked="{Binding FeatureExtractorTemplate.IsControlNetSupported}" Margin="0,18,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </UniformGrid>

                <UniformGrid Columns="3" Margin="0,5,0,0">
                    <StackPanel Margin="0,0,0,0">
                        <TextBlock Text="Normalize Input" Style="{StaticResource FieldTextBlockStyle}"/>
                        <ComboBox ItemsSource="{Binding Source={StaticResource ImageNormalizeType}}" SelectedItem="{Binding FeatureExtractorTemplate.NormalizeType}"  />
                    </StackPanel>

                    <StackPanel Margin="5,0,0,0">
                        <TextBlock Text="Normalize Output" Style="{StaticResource FieldTextBlockStyle}"/>
                        <ComboBox ItemsSource="{Binding Source={StaticResource ImageNormalizeType}}" SelectedItem="{Binding FeatureExtractorTemplate.NormalizeOutputType}"  />
                    </StackPanel>

                </UniformGrid>

                <UniformGrid Columns="3" Margin="0,5,0,0">
                    <StackPanel Margin="0,0,0,0">
                        <TextBlock Text="Input Resize Mode" Style="{StaticResource FieldTextBlockStyle}"/>
                        <ComboBox ItemsSource="{Binding Source={StaticResource ImageResizeMode}}" SelectedItem="{Binding FeatureExtractorTemplate.InputResizeMode}"  />
                    </StackPanel>

                    <StackPanel Margin="5,4,0,0">
                        <TextBlock Text=""/>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding FeatureExtractorTemplate.SetOutputToInputAlpha}" />
                            <TextBlock Text="Result To Alpha"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Margin="5,4,0,0">
                        <TextBlock Text=""/>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding FeatureExtractorTemplate.InvertOutput}" />
                            <TextBlock Text="Invert Output"/>
                        </StackPanel>
                    </StackPanel>
                </UniformGrid>

            </StackPanel>

            <StackPanel Margin="5">


                <ItemsControl ItemsSource="{Binding ValidationResults}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:ValidationResult}">
                            <StackPanel Orientation="Horizontal">
                                <userControls:FontAwesome >
                                    <userControls:FontAwesome.Style>
                                        <Style TargetType="{x:Type userControls:FontAwesome}">
                                            <Setter Property="Color" Value="Red" />
                                            <Setter Property="Icon" Value="&#xf00d;" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsValid}" Value="True">
                                                    <Setter Property="Color" Value="LimeGreen" />
                                                    <Setter Property="Icon" Value="&#xf00c;" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </userControls:FontAwesome.Style>
                                </userControls:FontAwesome>
                                <TextBlock Text="{Binding Name}" Margin="5,0,0,3" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </StackPanel>


        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <UniformGrid Columns="2" Height="30">
                <Button Content="Save" Command="{Binding SaveCommand}" IsDefault="True"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100"/>
            </UniformGrid>
        </StackPanel>
    </DockPanel>
</local:BaseDialog>