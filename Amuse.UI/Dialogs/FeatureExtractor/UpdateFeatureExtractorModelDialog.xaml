<local:BaseDialog x:Class="Amuse.UI.Dialogs.UpdateFeatureExtractorModelDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Amuse.UI.Dialogs"
        xmlns:models="clr-namespace:Amuse.UI.Models"
        xmlns:userControls="clr-namespace:Amuse.UI.UserControls"
        mc:Ignorable="d"
        x:Name="UI"
        Icon="/Images/Icon.png"
        MinWidth="450"
        MaxWidth="450"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        SnapsToDevicePixels="True"
        AllowsTransparency="True"
        WindowStyle="None"
        Style="{StaticResource BaseToolWindow}"
        Title="Update Feature Extractor Model">
    <DockPanel DataContext="{Binding ElementName=UI}" Margin="5">
        <StackPanel DockPanel.Dock="Top">

            <Border BorderBrush="Red" BorderThickness="1" CornerRadius="4">
                <StackPanel Orientation="Horizontal" Margin="30, 5" HorizontalAlignment="Center">
                    <userControls:FontAwesome Color="Red" Icon="&#xf00d;" />
                    <TextBlock Text="{Binding ValidationError}" Margin="5,0,0,3" />
                </StackPanel>
                <Border.Style>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ValidationError}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
            </Border>


            <GroupBox Header="Settings" Margin="5,0" >
                <StackPanel Margin="5,0,5,5" >

                    <StackPanel Margin="0,5,0,0">
                        <TextBlock Text="Model Name" Style="{StaticResource FieldTextBlockStyle}"/>
                        <TextBox Text="{Binding UpdateModelSet.Name, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>

                    <StackPanel Margin="0,5,0,0">
                        <TextBlock Text="Model File" Style="{StaticResource FieldTextBlockStyle}"/>
                        <userControls:FilePickerTextBox FileName="{Binding UpdateModelSet.ModelFile, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Filter="Onnx files (*.onnx)|*.onnx" DefaultExt="onnx" />
                    </StackPanel>

                    <UniformGrid Columns="3" Margin="0,5,0,0">

                        <StackPanel Margin="0,0,0,0">
                            <TextBlock Text="Sample Size" Style="{StaticResource FieldTextBlockStyle}"/>
                            <TextBox Text="{Binding UpdateModelSet.ModelTemplate.SampleSize}" />
                        </StackPanel>

                        <StackPanel Margin="5,0,0,0">
                            <TextBlock Text="OutputChannels" Style="{StaticResource FieldTextBlockStyle}"/>
                            <TextBox Text="{Binding UpdateModelSet.ModelTemplate.OutputChannels}" />
                        </StackPanel>

                        <StackPanel Margin="5,0,0,0">
                            <TextBlock Text="" Style="{StaticResource FieldTextBlockStyle}"/>
                            <CheckBox Content="ControlNet Support" IsChecked="{Binding IsControlNetSupported}" />
                        </StackPanel>
                    </UniformGrid>

                    <UniformGrid Columns="3" Margin="0,5,0,0" >

                        <StackPanel Margin="0,0,0,0">
                            <TextBlock Text="Normalize Input" Style="{StaticResource FieldTextBlockStyle}"/>
                            <ComboBox ItemsSource="{Binding Source={StaticResource ImageNormalizeType}}" SelectedItem="{Binding UpdateModelSet.ModelTemplate.NormalizeType}"  />
                        </StackPanel>

                        <StackPanel Margin="5,0,0,0">
                            <TextBlock Text="Normalize Output" Style="{StaticResource FieldTextBlockStyle}"/>
                            <ComboBox ItemsSource="{Binding Source={StaticResource ImageNormalizeType}}" SelectedItem="{Binding UpdateModelSet.ModelTemplate.NormalizeOutputType}"  />
                        </StackPanel>



                    </UniformGrid>

                    <UniformGrid Columns="3" Margin="0,5,0,0">
                        <StackPanel Margin="0,0,0,0">
                            <TextBlock Text="InputResizeMode" Style="{StaticResource FieldTextBlockStyle}"/>
                            <ComboBox ItemsSource="{Binding Source={StaticResource ImageResizeMode}}" SelectedItem="{Binding UpdateModelSet.ModelTemplate.InputResizeMode}"  />
                        </StackPanel>

                        <StackPanel Margin="5,0,0,0">
                            <TextBlock Text="" Style="{StaticResource FieldTextBlockStyle}"/>
                            <StackPanel Orientation="Horizontal">
                                <CheckBox IsChecked="{Binding UpdateModelSet.ModelTemplate.SetOutputToInputAlpha}" />
                                <TextBlock Text="Result To Alpha"/>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Margin="5,0,0,0">
                            <TextBlock Text="" Style="{StaticResource FieldTextBlockStyle}"/>
                            <StackPanel Orientation="Horizontal">
                                <CheckBox IsChecked="{Binding UpdateModelSet.ModelTemplate.InvertOutput}" />
                                <TextBlock Text="Invert Output"/>
                            </StackPanel>
                        </StackPanel>

                    </UniformGrid>

                </StackPanel>
            </GroupBox>


            <GroupBox Margin="5,5,5,0" >

                <StackPanel>
                    <TextBlock Text="Execution Device" Style="{StaticResource FieldTextBlockStyle}"/>
                    <userControls:DevicePickerControl
                         DeviceId="{Binding UpdateModelSet.DeviceId, Mode=TwoWay}"
                         Provider="{Binding UpdateModelSet.ExecutionProvider, Mode=TwoWay}" />
                </StackPanel>

            </GroupBox>

        </StackPanel>

        <DockPanel DockPanel.Dock="Bottom"  Margin="0,20,0,0" >
            <UniformGrid DockPanel.Dock="Right" Columns="2" Height="30">
                <Button Content="Save" Command="{Binding SaveCommand}" IsDefault="True"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100"/>
            </UniformGrid>

            <TextBlock />
        </DockPanel>

    </DockPanel>
</local:BaseDialog>