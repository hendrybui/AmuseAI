<UserControl x:Class="Amuse.UI.UserControls.CachedMedia"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:effects="clr-namespace:Amuse.UI.Effects"
             xmlns:local="clr-namespace:Amuse.UI.UserControls"
             Name="UI">
    <Grid DataContext="{Binding ElementName=UI}">
        <Image Source="{Binding ImageSource, Converter={StaticResource NullImageConverter}}" Visibility="{Binding IsVideoSource, Converter={StaticResource InverseBooleanToVisibilityConverter}}" Stretch="{Binding Stretch}"/>
        <MediaElement
            x:Name="MediaElementControl"
            Volume="0"
            Source="{Binding VideoSource, Converter={StaticResource NullImageConverter}}"
            Stretch="{Binding Stretch}"
            ScrubbingEnabled="True"
            LoadedBehavior="Manual" 
            UnloadedBehavior="Close" 
            Loaded="MediaElement_Loaded"
            MediaOpened="MediaElement_MediaOpened" 
            MediaEnded="MediaElement_MediaEnded" 
            MouseLeftButtonDown="MediaElement_MouseDown"
            Visibility="{Binding IsVideoSource, Converter={StaticResource BooleanToVisibilityConverter}}">
        </MediaElement>
    </Grid>
</UserControl>
