<UserControl x:Class="Amuse.UI.UserControls.ProgressElement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:behaviors="clr-namespace:Amuse.UI.Behaviors"
             Name="ProgressElementUI">
    <Grid DataContext="{Binding ElementName=ProgressElementUI}">
        <ProgressBar x:Name="ProgressBarControl" Maximum="{Binding Progress.Maximum}" behaviors:SmoothProgressBarBehavior.SmoothValue="{Binding Progress.Value}" HorizontalAlignment="Center" >
            <ProgressBar.Style>
                <Style TargetType="{x:Type ProgressBar}" BasedOn="{StaticResource {x:Type ProgressBar}}">
                    <Setter Property="IsIndeterminate" Value="False"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Progress.Maximum, ElementName=ProgressElementUI}" Value="-1">
                            <Setter Property="IsIndeterminate" Value="True"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ProgressBar.Style>
        </ProgressBar>
        <TextBlock Text="{Binding Progress.Message}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="13"/>
    </Grid>
</UserControl>
