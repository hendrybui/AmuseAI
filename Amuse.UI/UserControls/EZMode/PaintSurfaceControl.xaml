<UserControl x:Class="Amuse.UI.UserControls.PaintSurfaceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:colorPicker="clr-namespace:ColorPicker;assembly=ColorPicker"
             xmlns:local="clr-namespace:Amuse.UI.UserControls"
             xmlns:userControls="clr-namespace:Amuse.UI.UserControls"
             xmlns:behaviors="clr-namespace:Amuse.UI.Behaviors"
             mc:Ignorable="d" 
             d:DesignWidth="552" 
             d:DesignHeight="552" 
             Name="UI" 
             Focusable="True"
             AllowDrop="True">
    <UserControl.InputBindings>
        <KeyBinding Modifiers="Ctrl" Key="O" Command="{Binding LoadCanvasCommand, ElementName=UI}" />
        <KeyBinding Modifiers="Ctrl" Key="C" Command="{Binding CopyCanvasCommand, ElementName=UI}" />
        <KeyBinding Modifiers="Ctrl" Key="V" Command="{Binding PasteCanvasCommand, ElementName=UI}" />
        <KeyBinding Modifiers="Ctrl" Key="S" Command="{Binding SaveCanvasCommand, ElementName=UI}" />
        <KeyBinding Modifiers="Ctrl" Key="Z" Command="{Binding UndoCanvasCommand, ElementName=UI}" />
        <KeyBinding Modifiers="Ctrl" Key="Y" Command="{Binding RedoCanvasCommand, ElementName=UI}" />
    </UserControl.InputBindings>
    <Border DataContext="{Binding ElementName=UI}" BorderBrush="{StaticResource ControlBrightDefaultBorderBrush}" BorderThickness="1"  >
        <DockPanel>

            <!--Leftbar-->
            <DockPanel DockPanel.Dock="Left" Width="44" >

                <UniformGrid DockPanel.Dock="Top" Rows="10" Margin="2">

                    <!--Refresh-->
                    <Grid>
                        <Button  Command="{Binding GenerateCommand}" Style="{StaticResource SuccessButtonStyle}" Visibility="{Binding IsGenerating, Converter={StaticResource InverseBooleanToVisibilityConverter}}"  ToolTip="Live Generation" >
                            <userControls:FontAwesome Icon="&#xe2ca;" IconStyle="Light" Color="{StaticResource SuccessColour}"/>
                        </Button>
                        <Button Command="{Binding GenerateCancelCommand}" Style="{StaticResource WarningButtonStyle}" Visibility="{Binding IsGenerating, Converter={StaticResource BooleanToVisibilityConverter}}" ToolTip="Cancel Generation" >
                            <userControls:FontAwesome Icon="&#xf00d;" IconStyle="Light" Color="{StaticResource WarningColour}"/>
                        </Button>
                    </Grid>

                    <!--RandomizeSeed-->
                    <Button Command="{Binding RandomizeSeedCommand}" Style="{StaticResource TransparentButton}" Visibility="{Binding IsRealtimeEnabled, Converter={StaticResource BooleanToHiddenConverter}}" Margin="5,0,5,15" ToolTip="New Image Variant">
                        <userControls:FontAwesome Icon="&#xf021;" IconStyle="Light" Size="13"  />
                    </Button>

                    <!--RoundBrush-->
                    <Button Command="{Binding SelectToolCommand}" CommandParameter="{x:Static local:PaintDrawingTool.RoundBrush}" Height="40" ToolTip="Round Paint Brush">
                        <userControls:FontAwesome Icon="&#xf1fc;" IconStyle="Light"  >
                            <userControls:FontAwesome.Style>
                                <Style TargetType="{x:Type userControls:FontAwesome}" >
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Setter Property="Color" Value="{StaticResource ButtonForeground}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CanvasDrawingTool, ElementName=UI}" Value="RoundBrush" >
                                            <Setter Property="BorderThickness" Value="1" />
                                            <Setter Property="Color" Value="{StaticResource ControlPrimaryColourBackground}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </userControls:FontAwesome.Style>
                        </userControls:FontAwesome>
                        <Button.Style>
                            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}" >
                                <Setter Property="BorderThickness" Value="1" />
                                <Setter Property="BorderBrush" Value="{StaticResource ButtonBorderBrush}" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CanvasDrawingTool, ElementName=UI}" Value="RoundBrush" >
                                        <Setter Property="BorderThickness" Value="1" />
                                        <Setter Property="BorderBrush" Value="{StaticResource ControlPrimaryColourBackground}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>


                    <!--SquareBrush-->
                    <Button Command="{Binding SelectToolCommand}" CommandParameter="{x:Static local:PaintDrawingTool.SquareBrush}" Height="40" ToolTip="Square Paint Brush">
                        <userControls:FontAwesome Icon="&#xf55d;" IconStyle="Light"  >
                            <userControls:FontAwesome.Style>
                                <Style TargetType="{x:Type userControls:FontAwesome}" >
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Setter Property="Color" Value="{StaticResource ButtonForeground}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CanvasDrawingTool, ElementName=UI}" Value="SquareBrush" >
                                            <Setter Property="BorderThickness" Value="1" />
                                            <Setter Property="Color" Value="{StaticResource ControlPrimaryColourBackground}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </userControls:FontAwesome.Style>
                        </userControls:FontAwesome>
                        <Button.Style>
                            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}" >
                                <Setter Property="BorderThickness" Value="1" />
                                <Setter Property="BorderBrush" Value="{StaticResource ButtonBorderBrush}" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CanvasDrawingTool, ElementName=UI}" Value="SquareBrush" >
                                        <Setter Property="BorderThickness" Value="1" />
                                        <Setter Property="BorderBrush" Value="{StaticResource ControlPrimaryColourBackground}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>


                    <!--Highlighter-->
                    <Button Command="{Binding SelectToolCommand}" CommandParameter="{x:Static local:PaintDrawingTool.Highlight}" ToolTip="Highlighter Pen">
                        <userControls:FontAwesome Icon="&#xf591;" IconStyle="Light"  >
                            <userControls:FontAwesome.Style>
                                <Style TargetType="{x:Type userControls:FontAwesome}" >
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Setter Property="Color" Value="{StaticResource ButtonForeground}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CanvasDrawingTool, ElementName=UI}" Value="Highlight" >
                                            <Setter Property="BorderThickness" Value="1" />
                                            <Setter Property="Color" Value="{StaticResource ControlPrimaryColourBackground}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </userControls:FontAwesome.Style>
                        </userControls:FontAwesome>
                        <Button.Style>
                            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}" >
                                <Setter Property="BorderThickness" Value="1" />
                                <Setter Property="BorderBrush" Value="{StaticResource ButtonBorderBrush}" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CanvasDrawingTool, ElementName=UI}" Value="Highlight" >
                                        <Setter Property="BorderThickness" Value="1" />
                                        <Setter Property="BorderBrush" Value="{StaticResource ControlPrimaryColourBackground}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>


                    <!--Eraser-->
                    <Button Command="{Binding SelectToolCommand}" CommandParameter="{x:Static local:PaintDrawingTool.Eraser}" ToolTip="Eraser">
                        <userControls:FontAwesome Icon="&#xf12d;" IconStyle="Light"  >
                            <userControls:FontAwesome.Style>
                                <Style TargetType="{x:Type userControls:FontAwesome}" >
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Setter Property="Color" Value="{StaticResource ButtonForeground}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CanvasDrawingTool, ElementName=UI}" Value="Eraser" >
                                            <Setter Property="BorderThickness" Value="1" />
                                            <Setter Property="Color" Value="{StaticResource ControlPrimaryColourBackground}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </userControls:FontAwesome.Style>
                        </userControls:FontAwesome>
                        <Button.Style>
                            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}" >
                                <Setter Property="BorderThickness" Value="1" />
                                <Setter Property="BorderBrush" Value="{StaticResource ButtonBorderBrush}" />

                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CanvasDrawingTool, ElementName=UI}" Value="Eraser" >
                                        <Setter Property="BorderThickness" Value="1" />
                                        <Setter Property="BorderBrush" Value="{StaticResource ControlPrimaryColourBackground}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>


                    <!--RandomSeed-->
                    <Grid ToolTip="Create new variant each generation">
                        <ToggleButton IsChecked="{Binding IsRandomSeedEnabled}"  Visibility="{Binding IsRealtimeEnabled, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                            <userControls:FontAwesome Icon="&#xf074;" IconStyle="Light" Color="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ToggleButton}}}" />
                        </ToggleButton>
                        <ToggleButton IsChecked="False"  IsEnabled="False" Visibility="{Binding IsRealtimeEnabled, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <userControls:FontAwesome Icon="&#xf074;" IconStyle="Light" Color="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ToggleButton}}}" />
                        </ToggleButton>
                    </Grid>


                    <!--FitToCurve-->
                    <ToggleButton IsChecked="{Binding IsFitToCurveEnabled}"  ToolTip="Fit to curve">
                        <userControls:FontAwesome Icon="&#xe23f;" IconStyle="Light" Color="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ToggleButton}}}" />
                    </ToggleButton>


                    <!--Pressure-->
                    <ToggleButton IsChecked="{Binding IsPressureEnabled}" ToolTip="Enable stylus pressure support" >
                        <userControls:FontAwesome Icon="&#xe211;" IconStyle="Light" Color="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ToggleButton}}}" />
                    </ToggleButton>

                </UniformGrid>

                <DockPanel DockPanel.Dock="Bottom" Height="40" Margin="2">
                    <!--Bottom Left Corner-->
                </DockPanel>

                <!--ToolSize-->
                <DockPanel  Margin="2" >
                    <TextBlock DockPanel.Dock="Top" Text="Tool Size" FontSize="8" Margin="5,-4,6,0" Opacity=".6" FontStyle="Italic" HorizontalAlignment="Center"/>
                    <TextBlock DockPanel.Dock="Bottom" Text="{Binding DrawingToolSize, StringFormat={}{0}}"  FontSize="8" Opacity=".6" FontWeight="Medium" HorizontalAlignment="Center" />
                    <Slider HorizontalAlignment="Center" Value="{Binding DrawingToolSize}" Orientation="Vertical" Minimum="1" Maximum="100" TickFrequency="1" IsSnapToTickEnabled="True" SmallChange="1" LargeChange="5" >
                        <i:Interaction.Behaviors>
                            <behaviors:SliderMouseWheelBehavior />
                        </i:Interaction.Behaviors>
                    </Slider>
                </DockPanel>

            </DockPanel>


            <!--TopBar-->
            <DockPanel DockPanel.Dock="Top"  Height="44">

                <!--ColorPicker-->
                <Border DockPanel.Dock="Left" BorderBrush="{StaticResource ButtonBorderDefault}" BorderThickness="1" Width="40" Margin="5,2">
                    <colorPicker:PortableColorPicker  
                         SelectedColor="{Binding SelectedColor, Mode=TwoWay}" 
                         IsPickerOpen="{Binding IsPickerOpen, Mode=OneWayToSource}"  
                         Background="{StaticResource ContainerBackground}" />
                </Border>


                <!--Recent Colors-->
                <ListBox  DockPanel.Dock="Left" ItemsSource="{Binding RecentColors}" SelectedValue="{Binding SelectedColor, Mode=TwoWay}" Background="Transparent" BorderBrush="Transparent" SnapsToDevicePixels="True">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="{x:Type ListBoxItem}">
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Background" Value="{Binding ., Converter={StaticResource ColorToBrushConverter}}" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                        <Border BorderThickness="1" Width="20"  Background="{TemplateBinding Background}">
                                            <GridViewRowPresenter HorizontalAlignment="Stretch" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"  Margin="0" Content="{TemplateBinding Content}"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="2" Columns="8" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                </ListBox>


                <!--Fill-->
                <Button DockPanel.Dock="Left"  Command="{Binding FillCanvasCommand}" Style="{StaticResource TransparentButton}" Margin="4" ToolTip="Fill Background">
                    <userControls:FontAwesome Icon="&#xf576;" IconStyle="Light"  />
                </Button>


                <!--Image-->
                <Button DockPanel.Dock="Left" Command="{Binding LoadCanvasCommand}"  Style="{StaticResource TransparentButton}" Margin="4" ToolTip="Load Image Background">
                    <userControls:FontAwesome Icon="&#xf03e;" IconStyle="Light"  />
                </Button>


                <!--Clear-->
                <Button DockPanel.Dock="Left" Command="{Binding ClearCanvasCommand}" Style="{StaticResource TransparentButton}" Margin="4" ToolTip="Clear Drawing">
                    <userControls:FontAwesome Icon="&#xf05e;" IconStyle="Light" Color="{StaticResource DangerColour}"  />
                </Button>


                <!--OverlayOpacity-->
                <DockPanel Margin="10,4,0,0">
                    <DockPanel DockPanel.Dock="Top">
                        <TextBlock DockPanel.Dock="Left" Text="Overlay" FontSize="8" Opacity=".6" FontStyle="Italic" Margin="4,0,0,0"/>
                        <TextBlock DockPanel.Dock="Right" Text="{Binding OverlayOpacity, StringFormat={}{0:F0}%, Converter={StaticResource MutiplyByConverter}, ConverterParameter=100}"  FontSize="8" Opacity=".6" FontWeight="Medium" TextAlignment="Right" Margin="0,0,5,0"/>
                    </DockPanel>
                    <Slider Value="{Binding OverlayOpacity}" Minimum="0" Maximum="1" TickFrequency="0.01" IsSnapToTickEnabled="True" SmallChange="0.01" LargeChange="0.1">
                        <i:Interaction.Behaviors>
                            <behaviors:SliderMouseWheelBehavior />
                        </i:Interaction.Behaviors>
                    </Slider>
                </DockPanel>

            </DockPanel>


            <!--BottomBar-->
            <DockPanel DockPanel.Dock="Bottom" Height="40">
                <UniformGrid Columns="2">

                    <!--GenerateSteps-->
                    <DockPanel Margin="10,4,0,0">
                        <DockPanel DockPanel.Dock="Top">
                            <TextBlock DockPanel.Dock="Left" Text="Quality"  TextWrapping="Wrap" FontSize="8" Opacity=".6" FontStyle="Italic" Margin="4,0,0,0"/>
                            <TextBlock DockPanel.Dock="Right" Text="{Binding GenerateSteps, StringFormat={}{0}}"  FontSize="8" Opacity=".6" FontWeight="Medium" TextAlignment="Right" Margin="0,0,5,0"/>
                        </DockPanel>
                        <Slider Value="{Binding GenerateSteps}" Minimum="2" Maximum="50" TickFrequency="1" IsSnapToTickEnabled="True" SmallChange="1" LargeChange="1" >
                            <i:Interaction.Behaviors>
                                <behaviors:SliderMouseWheelBehavior />
                            </i:Interaction.Behaviors>
                        </Slider>
                    </DockPanel>

                    <Grid>

                        <!--GenerateStrength-->
                        <DockPanel  Margin="10,4,0,0" Visibility="{Binding IsControlNetEnabled, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                            <DockPanel DockPanel.Dock="Top">
                                <TextBlock DockPanel.Dock="Left" Text="Strength"  TextWrapping="Wrap" FontSize="8" Opacity=".6" FontStyle="Italic" Margin="4,0,0,0"/>
                                <TextBlock DockPanel.Dock="Right" Text="{Binding GenerateStrength, StringFormat={}{0:F0}%, Converter={StaticResource MutiplyByConverter}, ConverterParameter=100}"  FontSize="8" Opacity=".6" FontWeight="Medium" TextAlignment="Right" Margin="0,0,5,0"/>
                            </DockPanel>
                            <Slider Value="{Binding GenerateStrength}" Minimum="0" Maximum="1" TickFrequency="0.01" IsSnapToTickEnabled="True" SmallChange="0.01" LargeChange="0.1" >
                                <i:Interaction.Behaviors>
                                    <behaviors:SliderMouseWheelBehavior />
                                </i:Interaction.Behaviors>
                            </Slider>
                        </DockPanel>


                        <!--AssistantStrength-->
                        <DockPanel  Margin="10,4,0,0" Visibility="{Binding IsControlNetEnabled, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <DockPanel DockPanel.Dock="Top">
                                <TextBlock DockPanel.Dock="Left" Text="Strength"  TextWrapping="Wrap" FontSize="8" Opacity=".6" FontStyle="Italic" Margin="4,0,0,0"/>
                                <TextBlock DockPanel.Dock="Right" Text="{Binding AssistantStrength, StringFormat={}{0:F0}%, Converter={StaticResource MutiplyByConverter}, ConverterParameter=100}"  FontSize="8" Opacity=".6" FontWeight="Medium" TextAlignment="Right" Margin="0,0,5,0"/>
                            </DockPanel>
                            <Slider Value="{Binding AssistantStrength}" Minimum="0" Maximum="1" TickFrequency="0.01" IsSnapToTickEnabled="True" SmallChange="0.01" LargeChange="0.1" >
                                <i:Interaction.Behaviors>
                                    <behaviors:SliderMouseWheelBehavior />
                                </i:Interaction.Behaviors>
                            </Slider>
                        </DockPanel>

                    </Grid>


                </UniformGrid>

            </DockPanel>


            <!--Surface-->
            <Border Background="#40000000" Padding="5" >

                <Viewbox StretchDirection="{Binding ZoomDirection}" UseLayoutRounding="True" RenderOptions.BitmapScalingMode="Fant">
                    <Grid x:Name="SurfaceCanvas">
                        <InkCanvas x:Name="PaintCanvas"  
                             Background="White"
                             ForceCursor="True" 
                             Cursor="Pen"
                             PreviewMouseLeftButtonUp="PaintCanvas_MouseLeftButtonUp"
                             PreviewMouseLeftButtonDown="PaintCanvas_MouseLeftButtonDown"
                             PreviewMouseMove="PaintCanvas_PreviewMouseMove"
                             PreviewMouseWheel="PaintCanvas_OnMouseWheel"
                             EditingMode="{Binding CanvasEditingMode}" 
                             Width="{Binding SurfaceWidth}" 
                             Height="{Binding SurfaceHeight}" 
                             DefaultDrawingAttributes="{Binding BrushAttributes}" >
                        </InkCanvas>

                        <Canvas x:Name="PaintCanvasToolSurface" 
                            IsHitTestVisible="False"
                            ClipToBounds="True"
                            Background="Transparent"
                            Width="{Binding SurfaceWidth}" 
                            Height="{Binding SurfaceHeight}" >
                            <Rectangle 
                                Stroke="Black"
                                StrokeThickness="1"
                                Canvas.Left="{Binding ToolOutlineX}"
                                Canvas.Top="{Binding ToolOutlineY}" 
                                Width="{Binding BrushAttributes.Width}" 
                                Height="{Binding BrushAttributes.Height}" 
                                Visibility="{Binding ToolOutlineVisibility}">
                                <Rectangle.Fill>
                                    <SolidColorBrush Color="{Binding SelectedColor}" />
                                </Rectangle.Fill>
                                <Rectangle.Style>
                                    <Style TargetType="{x:Type Rectangle}" >
                                        <Setter Property="RadiusX" Value="0" />
                                        <Setter Property="RadiusY" Value="0" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding BrushAttributes.StylusTip, ElementName=UI}" Value="Ellipse" >
                                                <Setter Property="RadiusX" Value="10000" />
                                                <Setter Property="RadiusY" Value="10000" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Rectangle.Style>
                            </Rectangle>
                            <Canvas.Style>
                                <Style TargetType="{x:Type Canvas}" >
                                    <Setter Property="Visibility" Value="Hidden" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMouseOver, ElementName=PaintCanvas}" Value="True" >
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Canvas.Style>
                        </Canvas>
                        <Image 
                            IsHitTestVisible="False"
                            Source="{Binding OverlayImage}" 
                            Opacity="{Binding OverlayOpacity}"
                            Width="{Binding SurfaceWidth}" 
                            Height="{Binding SurfaceHeight}" />
                        <Grid.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Open" InputGestureText="Ctrl+O" Command="{Binding LoadCanvasCommand}" >
                                    <MenuItem.Icon>
                                        <userControls:FontAwesome Icon="&#xf1c5;" IconStyle="Light" Size="15" />
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="Copy" InputGestureText="Ctrl+C" Command="{Binding CopyCanvasCommand}" >
                                    <MenuItem.Icon>
                                        <userControls:FontAwesome Icon="&#xf0c5;" IconStyle="Light" Size="15" />
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="Paste" InputGestureText="Ctrl+V" Command="{Binding PasteCanvasCommand}" >
                                    <MenuItem.Icon>
                                        <userControls:FontAwesome Icon="&#xf0ea;" IconStyle="Light" Size="15" />
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="Save" InputGestureText="Ctrl+S" Command="{Binding SaveCanvasCommand}" >
                                    <MenuItem.Icon>
                                        <userControls:FontAwesome Icon="&#xf0c7;" IconStyle="Light" Size="15" />
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="Undo" InputGestureText="Ctrl+Z" Command="{Binding UndoCanvasCommand}"  >
                                    <MenuItem.Icon>
                                        <userControls:FontAwesome Icon="&#xf2ea;" IconStyle="Light" Size="15" />
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="Redo" InputGestureText="Ctrl+Y" Command="{Binding RedoCanvasCommand}"  >
                                    <MenuItem.Icon>
                                        <userControls:FontAwesome Icon="&#xf2f9;" IconStyle="Light" Size="15" />
                                    </MenuItem.Icon>
                                </MenuItem>
                            </ContextMenu>
                        </Grid.ContextMenu>
                    </Grid>
                </Viewbox>

            </Border>

        </DockPanel>
    </Border>
</UserControl>
