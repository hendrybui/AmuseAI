<Window x:Class="Amuse.UI.Splashscreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="NoResize"
        Height="500"
        Width="800"
        Background="Transparent" 
        Foreground="#FFC8C8C8"
        WindowStartupLocation="CenterScreen" 
        Icon="/Images/Icon.ico" 
        Cursor="AppStarting" 
        Focusable="False"
        IsHitTestVisible="False"
        IsTabStop="False"
        Name="UI">
    <Grid DataContext="{Binding ElementName=UI}" UseLayoutRounding="True" >
        <Rectangle Fill="#FF272727" Width="600" Height="350" Stroke="#80d3ae58" StrokeThickness="1"/>
        <Image Source="/Images/Splash.png" SnapsToDevicePixels="True" RenderOptions.BitmapScalingMode="Fant"/>
        <Image Source="/Images/Splash_Logo.png" Width="200" Margin="301,0,299,60" Opacity=".95" SnapsToDevicePixels="True" RenderOptions.BitmapScalingMode="Fant"/>
        <Image Source="/Images/Splash_AMD.png" Margin="615,371,118,91" Opacity=".95" SnapsToDevicePixels="True" RenderOptions.BitmapScalingMode="Fant" Stretch="Uniform"/>
        <TextBlock DockPanel.Dock="Bottom" Text="{Binding Title}" TextAlignment="Center" FontSize="15" Margin="0,337,0,0" VerticalAlignment="Top" SnapsToDevicePixels="True" RenderOptions.BitmapScalingMode="Fant"/>
    </Grid>
</Window>
